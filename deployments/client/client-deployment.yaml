---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client
  template:
    metadata:
      labels:
        app: client
    spec:
      containers:
        - name: client
          image: k8s-sandbox-client:local
          imagePullPolicy: Never
          env:
            - name: GRPC_ADDR
              value: "api:50051"
          command: ["./client"]
